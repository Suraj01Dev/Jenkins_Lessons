currentBuild.displayname = "Docker_Build_Push-#"+currentBuild.number
pipeline{
    agent{
        label "jenkins-ssh-agent-docker"
    }
    stages{
        stage("Welcome")
        {
            steps{
            sh 'echo "Welcome" '
            }
        }

    }
}
