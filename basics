currentBuild.displayName="Basics-#"+currentBuild.number
pipeline{
    agent any;
    stages{
        stage("Welcome to Jenkins"){
            steps{
                sh "echo hi "
            }
        }
    }
}
